class Solution:
    def restoreString(self, s: str, indices: List[int]) -> str:
        res = ""
        
        ss = [n for n in range(len(s))]
        
        for i in ss:
            res += s[indices.index(i)]
        
        return res
